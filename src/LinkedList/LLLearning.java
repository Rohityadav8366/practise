package LinkedList;
public class LLLearning {
    Node head;
    class Node{
        int data;
        Node next;
        Node(int d){
            data=d;
            next=null;
        }
    }
    public void LLInsert(LL list,int data)
    {
        Node new_node=new Node(data);
        if(list.head==null)
        {
            list.head=new_
        }
    }
}
